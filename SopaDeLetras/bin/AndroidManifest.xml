<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.desenrolapp.sopadeletras"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="org.desenrolapp.sopadeletras.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.desenrolapp.tablero.pintaTablero"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="org.desenrolapp.sopadeletras.Juego"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
        </activity>

        <service android:name="tablero.ServicioTablero" >
        </service>

        <activity
            android:name="org.desenrolapp.sopadeletras.LeerBaseDatos"
            android:label="@string/title_activity_leer_base_datos" >
        </activity>
    </application>

</manifest>